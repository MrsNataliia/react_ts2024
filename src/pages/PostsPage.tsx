import React, {FC} from 'react';

import {Posts} from "../components/postsComponents/Posts";

const PostsPage: FC = () => {
    return (
        <div>
            <Posts/>
        </div>
    );
};

export {PostsPage};